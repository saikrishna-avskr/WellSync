

# Environment variables
python-dotenv

# HTTP requests
requests
urllib3

# Retry mechanism
#requests[socks]  # to support Retry and adapters explicitly if needed

# Flask API
flask
flask-cors

# Speech recognition and text-to-speech
SpeechRecognition
pyttsx3

# LangChain and related integrations
langchain
langchain-community
langchain-groq
langchain-huggingface
google-generativeai
# Vector store
faiss-cpu  # or faiss-gpu if using GPU

# HuggingFace Transformers/Embeddings
transformers
sentence-transformers  # if your embedding model uses this

# Optional: If using local documents with loaders
unstructured  # sometimes needed for loaders like TextLoader
